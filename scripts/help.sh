#!/bin/bash

color_reset="\033[0m"
color_info="\033[32m"
color_comment="\033[33m"

printf "${color_info}install${color_reset}		${color_comment}install application dependencies${color_reset} \n"
printf "${color_info}test${color_reset}		${color_comment}run unit test${color_reset} \n"
